<% path = URI.parse(request.original_url).path.split('/') %>
<ul class="nav nav-pills mb-3 mx-auto home-nav" id="pills-tab" role="tablist">
  <li class="nav-item" role="presentation">
    <%= link_to "Photos", page == 'feed' ? root_path : discovery_path, 
            class: "nav-link #{"active" unless path.include?('albums')}" %>
  </li>
  <li class="nav-item" role="presentation">
    <%= link_to "Albums", page == 'feed' ? feed_albums_path : discovery_albums_path,  
            class: "nav-link #{"active" if path.include?('albums')}" %>
  </li>
</ul>